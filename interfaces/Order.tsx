export interface Order {
    id: number;
    client_id: number | null; // Cambia el tipo según tu modelo
    user_id: number | null; // Cambia el tipo según tu modelo
    // Agrega otros campos que necesites
}